# To install:


apt install git -y  
git clone https://github.com/GluGGsel/workout-counter.git  
cd workout-counter  
sudo chmod +x install.sh  
sudo ./install.sh  


# Works so far in:
- ubuntu lxc on proxmox
